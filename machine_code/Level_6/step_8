### config/routes.rb

Rails.application.routes.draw do
	
	resources :users, only: [:create]
	resources :rides, only: [:create] do
		member do
			post :accept
			post :start
			post :complete
			post :cancel
		end
	end
end